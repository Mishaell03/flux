import 'package:front/futures/login/services/auth_callback.dart';

Future<void> handleWebAuthCallback(
    AuthCallbackCompleteService callbackService,
    ) async {
  // не вызывается на не-веб платформах
}